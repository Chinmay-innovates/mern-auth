<html lang="en">

<head>
	<meta charset="UTF-8" />
	<meta name="viewport" content="width=device-width, initial-scale=1.0" />
	<title>Home</title>
	<style>
		/* General Styles */
		body {
			font-family: Arial, sans-serif;
			background-color: #f3f4f6;
			margin: 0;
			padding: 0;
			display: flex;
			justify-content: center;
			align-items: center;
			min-height: 100vh;
		}

		h1 {
			text-align: center;
			color: #4b5563;
		}

		/* Card Container */
		.card-container {
			display: flex;
			flex-wrap: wrap;
			gap: 20px;
			justify-content: center;
		}

		/*
			Card Styles */
		.card {
			background: #ffffff;
			box-shadow: 0 4px 8px rgba(0,
					0, 0, 0.1);
			border-radius: 10px;
			padding: 20px;
			width: 300px;
			transition:
				transform 0.2s ease;
		}

		.card:hover {
			transform: scale(1.05);
		}

		.card h2 {
			color: #374151;
			font-size: 1.25rem;
			margin-bottom: 10px;
		}

		.card p {
			color: #6b7280;
			font-size: 0.9rem;
			margin: 5px 0;
		}

		.card span {
			font-weight: bold;
			color: #1f2937;
		}
	</style>
</head>

<body>
	<h1 style="position: relative; left: 500px; top: -280px ">Welcome ,
		{{#each user}}{{name}}{{/each}}</h1>
	<div class="card-container">
		{{#each user}}
		<div class="card">
			<h2>{{name}}</h2>
			<p><span>Password:</span> {{password}}</p>
			<p><span>Account Created At:</span> {{createdAt}}</p>
		</div>
		{{/each}}
	</div>
</body>

</html>
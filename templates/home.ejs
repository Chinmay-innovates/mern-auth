<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Home</title>
    <style>
		 @import url("https://fonts.googleapis.com/css2?family=Poppins:ital,wght@0,100;0,200;0,300;0,400;0,500;0,600;0,700;0,800;0,900;1,100;1,200;1,300;1,400;1,500;1,600;1,700;1,800;1,900&display=swap");
        /* General Styles */
        body {
			overflow: hidden;
            font-family: "Poppins", sans-serif;
			background-color: #1b498a;
            margin: 0;
            padding: 0;
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            color: #333;
            flex-direction: column;
            text-align: center;
        }

        /* Centered Hello Message */
        h1 {
            color: #968989;
            margin-bottom: 20px;
            font-size: 2rem;
        }

        /* Card Container */
        .card-container {
            display: flex;
            justify-content: center;
            align-items: center;
            flex-direction: column;
            gap: 20px;
            width: 100%;
            padding: 20px;
        }

        /* Card Styles */
        .card {
            background: #ffffff;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            border-radius: 10px;
            padding: 30px;
            max-width: 600px; /* Increased width for longer hash */
            width: 100%;
            word-wrap: break-word; /* Ensures long words wrap properly */
            text-align: left;
            transition: transform 0.3s ease, box-shadow 0.3s ease, filter 0.3s ease;
            cursor: pointer;
        }

        /* Hover Effects */
        .card:hover {
            transform: scale(1.05) rotate(2deg); /* Scale and tilt effect */
            box-shadow: 0 10px 20px rgba(0, 0, 0, 0.2); 
            filter: brightness(1.1); 
        }

        /* Card Content Styling */
        .card h2 {
            color: hsl(217, 100%, 94%);
            font-size: 1.5rem;
            margin-bottom: 15px;
        }

        .card p {
            color: #6b7280;
            font-size: 1rem;
            margin: 10px 0;
        }

        .card span {
            font-weight: bold;
            color: #1f2937;
        }

        /* Responsive Design */
        @media (max-width: 768px) {
            .card-container {
                padding: 10px;
            }

            .card {
                width: 90%;
                max-width: 100%;
            }
        }
    </style>
</head>

<body>
    <h1>Hello, <%= user.name %> 👋</h1>
    <div class="card-container">
        <div class="card">
            <h1><%= user.name %></h1>
            <p><span>Password:</span> <%= user.password %></p>
            <p><span>hash:</span> <%= user.hashedPassword %></p>
            <p><span>Account Created At:</span> <%= user.createdAt %></p>
        </div>
    </div>
</body>

</html>